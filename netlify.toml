# Next.js on Netlify - Explicit plugin ensures correct deployment
[[plugins]]
  package = "@netlify/plugin-nextjs"

[build]
  command = "npm run build"
  # Publish directory: Do NOT set. Let Next.js plugin handle it.
  # If UI has publish = ".next", clear it - that causes 404.
