# Next.js on Netlify - Zero config, Netlify auto-detects and uses OpenNext adapter
[build]
  command = "npm run build"

# If your Next.js app is in a subfolder (e.g. dcitc-website), set this in Netlify Dashboard:
# Site settings → Build & deploy → Build settings → Base directory = "dcitc-website"
